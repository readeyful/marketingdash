import PagePlaceholder from '../components/PagePlaceholder'

export default function Strategy() {
  return (
    <PagePlaceholder
      title="Strategy"
      description="The brand strategy doc will live here — a living, auto-saving rich text editor."
    />
  )
}
